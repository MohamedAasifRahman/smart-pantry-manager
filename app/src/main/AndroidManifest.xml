<?xml version="1.0" encoding="utf-8"?>
<!--
    Smart Pantry Manager.

    Note that this application declares NO permissions at all: no internet,
    no location and no storage. The assignment places Google Maps, mapping
    SDKs and GPS/location services explicitly out of scope, and the simplest
    proof of that is a manifest with nothing to grant.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartPantryManager"
        tools:targetApi="31">

        <activity
            android:name=".activity.PantryActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
